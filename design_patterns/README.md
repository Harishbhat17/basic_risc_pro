# Design Patterns

- `cdc/`: handshake + dual-FF synchronizer flow
- `fifo/`: synchronous FIFO and async FIFO architecture note
- `fsm/`: Mealy/Moore examples
- `arbiters/`: priority and round-robin arbitration
- `counters_timers/`: counters and tick timer examples
